package com.sportstalk.models.common;

/**
 * Enumerates possible reactions that are immediately supported.  Any string could do but it is better to enforce a set.
 * For now, we just provide "like"
 */
public enum Reaction {
    like
}
