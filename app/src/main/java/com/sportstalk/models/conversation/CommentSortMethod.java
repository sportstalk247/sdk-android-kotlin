package com.sportstalk.models.conversation;

/**
 * Used for comment sorting.  You can only sort by one method at a time.
 */
public enum CommentSortMethod {
    oldest,
    newest,
    mostReplies,
    votes
}
